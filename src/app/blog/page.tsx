import { BlogPage } from "@/components/blog-page";

export const metadata = {
  title: "Blog",
  description: "Sales automation, outbound strategy, and AI agent operations.",
};

export default function Blog() {
  return <BlogPage />;
}
