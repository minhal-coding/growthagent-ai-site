import { AboutPage } from "@/components/about-page";

export const metadata = {
  title: "About",
  description: "The story behind GrowthAgent AI.",
};

export default function About() {
  return <AboutPage />;
}
